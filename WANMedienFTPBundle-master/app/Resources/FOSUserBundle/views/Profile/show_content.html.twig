<div class="fos_user_user_show">
    <p>{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}: {{ user.username }}</p>

    <p>{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}: {{ user.email }}</p>
</div>
<a href="{{ path("fos_user_profile_edit") }}" class="btn"><i class="icon-pencil"></i> Profil bearbeiten</a>
<a href="{{ path("fos_user_change_password") }}" class="btn"><i class="icon-lock"></i> Passwort ändern</a><br/>