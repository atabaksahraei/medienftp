<form class="form-horizontal" action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-warning">{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</button>
</form>